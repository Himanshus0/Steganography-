{
  "name": "image-steganography-app",
  "version": "1.0.0",
  "description": "Full-stack image steganography app — classroom project",
  "private": true,
  "scripts": {
    "dev": "npm run kill-ports && concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "kill-ports": "lsof -ti:3001,5173 | xargs kill -9 2>/dev/null || true",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd server && npm test",
    "test:client": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
